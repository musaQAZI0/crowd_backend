const express = require('express');
const router = express.Router();

// Placeholder for finance routes
router.get('/', (req, res) => {
  res.json({ message: 'Finance endpoint - coming soon' });
});

module.exports = router;
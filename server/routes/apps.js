const express = require('express');
const router = express.Router();

// Placeholder for app routes
router.get('/', (req, res) => {
  res.json({ message: 'Apps endpoint - coming soon' });
});

module.exports = router;
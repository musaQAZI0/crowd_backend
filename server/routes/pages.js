const express = require('express');
const router = express.Router();

// Placeholder for page routes
router.get('/', (req, res) => {
  res.json({ message: 'Pages endpoint - coming soon' });
});

module.exports = router;